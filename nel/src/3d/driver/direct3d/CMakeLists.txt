FILE(GLOB SRC *.cpp)

DECORATE_NEL_LIB("nel_drv_direct3d")

ADD_LIBRARY(${LIBNAME} SHARED ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR} ${FREETYPE_INC})
TARGET_LINK_LIBRARIES(${LIBNAME} ${LIBXML2_LIBRARIES} ${FREETYPE_LIB})
SET_TARGET_PROPERTIES(${LIBNAME} PROPERTIES VERSION ${NL_VERSION})
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

