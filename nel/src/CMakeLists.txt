SUBDIRS(misc)

IF(WITH_3D)
  SUBDIRS(3d)
ENDIF(WITH_3D)

IF(WITH_GEORGES)
  SUBDIRS(georges)
ENDIF(WITH_GEORGES)

IF(WITH_LIGO)
  SUBDIRS(ligo)
ENDIF(WITH_LIGO)

IF(WITH_NET)
  SUBDIRS(net)
ENDIF(WITH_NET)

IF(WITH_SOUND)
  SUBDIRS(sound)
ENDIF(WITH_SOUND)
