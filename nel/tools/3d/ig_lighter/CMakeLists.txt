FILE(GLOB SRC *.cpp)

DECORATE_NEL_LIB("nelig_lighter_lib")
SET(NLIG_LIGHTER_LIB ${LIBNAME})

ADD_EXECUTABLE(ig_lighter ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(ig_lighter ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS} ${NLIG_LIGHTER_LIB})
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS ig_lighter RUNTIME DESTINATION bin)
