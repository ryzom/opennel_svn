FILE(GLOB SRC *.cpp *.h)

ADD_EXECUTABLE(get_neighbors ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(get_neighbors ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS})
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS get_neighbors RUNTIME DESTINATION bin)
