FILE(GLOB SRC *.cpp)

DECORATE_NEL_LIB("nelmisc")
SET(NLMISC_LIB ${LIBNAME})
DECORATE_NEL_LIB("nel3d")
SET(NL3D_LIB ${LIBNAME})

ADD_EXECUTABLE(nl_sample_clusterview ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(nl_sample_clusterview ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS} ${NLMISC_LIB} ${NL3D_LIB})
IF(WIN32)
  SET_TARGET_PROPERTIES(nl_sample_clusterview PROPERTIES LINK_FLAGS "/NODEFAULTLIB:libcmt /SUBSYSTEM:WINDOWS")
ENDIF(WIN32)
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS nl_sample_clusterview RUNTIME DESTINATION bin)
INSTALL(FILES main.cvs readme.txt DESTINATION share/nel/nl_sample_clusterview)
INSTALL(DIRECTORY groups/ 
	DESTINATION share/nel/nl_sample_clusterview/groups
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
INSTALL(DIRECTORY fonts/ 
	DESTINATION share/nel/nl_sample_clusterview/fonts
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
INSTALL(DIRECTORY max/ 
	DESTINATION share/nel/nl_sample_clusterview/max
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
INSTALL(DIRECTORY shapes/ 
	DESTINATION share/nel/nl_sample_clusterview/shapes
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
