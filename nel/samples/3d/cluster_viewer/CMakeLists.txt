FILE(GLOB SRC *.cpp)

DECORATE_NEL_LIB("nelmisc")
SET(NLMISC_LIB ${LIBNAME})
DECORATE_NEL_LIB("nel3d")
SET(NL3D_LIB ${LIBNAME})

ADD_EXECUTABLE(nl_clusterviewer ${SRC})

INCLUDE_DIRECTORIES(${LIBXML2_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(nl_clusterviewer ${LIBXML2_LIBRARIES} ${PLATFORM_LINKFLAGS} ${NLMISC_LIB} ${NL3D_LIB})
ADD_DEFINITIONS(${LIBXML2_DEFINITIONS})

INSTALL(TARGETS nl_clusterviewer RUNTIME DESTINATION bin)
INSTALL(FILES main.cvs readme.txt DESTINATION share/nel/nl_clusterviewer)
INSTALL(DIRECTORY groups/ 
	DESTINATION share/nel/nl_clusterviewer/groups
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
INSTALL(DIRECTORY fonts/ 
	DESTINATION share/nel/nl_clusterviewer/fonts
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
INSTALL(DIRECTORY max/ 
	DESTINATION share/nel/nl_clusterviewer/max
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
INSTALL(DIRECTORY shapes/ 
	DESTINATION share/nel/nl_clusterviewer/shapes
	PATTERN "CVS" EXCLUDE
	PATTERN ".svn" EXCLUDE
	PATTERN "Makefile*" EXCLUDE)
