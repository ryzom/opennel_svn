nel.sql was created by MySQL-Front.

It's the skeleton of the database needed for the login system (php and login_service).

First you have to create a database called "nel" and after inject the content
of nel.sql in "nel" database

lecroart@nevrax.com
